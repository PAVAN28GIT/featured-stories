<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Featured Stories</title>
    <link rel="stylesheet" href="/css/output.css">
</head>

<body class="bg-cyan-100 font-poppins">

    <!-- Navbar -->
    <div class="m-2">
        <%- include("partials/nav") %>
    </div>

    <!-- Main Section -->
    <div class="main flex flex-col items-center min-h-screen w-full mt-5 px-4 md:px-10 lg:px-20">

        <!-- Post Title -->
        <h1 class="text-1xl md:text-3xl lg:text-5xl text-black font-nunito font-bold mt-5 text-center">
            <%= post.title %>
        </h1>

        <!-- Back Link -->
        <div class="flex justify-between w-full mt-2 md:px-20 lg:px-44">
            <a class="text-cyan-950 text-sm md:text-lg lg:text-xl font-mono hover:underline" href="/profile">Go Back</a>
            <div></div> <!-- Empty div for spacing -->
        </div>

        <!-- Post Content -->
        <div class="w-full md:w-4/5 lg:w-3/4 p-6 md:p-8 lg:p-10 mt-5 bg-zinc-700 text-white rounded-lg shadow-lg">
            <p class="text-sm md:text-lg lg:text-xl font-nunito text-center">
                <%= post.body %>
            </p>
        </div>

        <!-- Action Buttons: Update & Delete -->
        <div class="flex flex-col md:flex-row justify-between w-full md:w-4/5 lg:w-3/4 mt-5 space-y-4 md:space-y-0 md:space-x-4">
            <a class="bg-cyan-900 text-white text-lg md:text-xl font-semibold py-2 px-4 rounded hover:bg-cyan-700 transition text-center" href="/profile/update/<%= post._id %>">Update</a>
            <a class="bg-red-600 text-white text-lg md:text-xl font-semibold py-2 px-4 rounded hover:bg-red-500 transition text-center" href="/profile/delete/<%= post._id %>">Delete</a>
        </div>

    </div>

</body>

</html>
